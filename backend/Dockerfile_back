# ─────────────────
# Stage 1 — Base
# ─────────────────
FROM node:20-alpine AS base
WORKDIR /app
COPY package*.json ./
RUN npm ci

# ─────────────────
# Stage 2 — dev
# ─────────────────
FROM base AS dev
CMD ["npx", "tsx", "watch", "src/app.ts"]